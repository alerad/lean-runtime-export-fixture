# lean-runtime export fixture

This unrelated public Lake project is the end-to-end acceptance fixture for
publishing an existing Lean project with
[`alerad/lean-runtime`](https://github.com/alerad/lean-runtime). It deliberately
depends on Mathlib and exposes `LeanRuntimeExportFixture` as its public module.
